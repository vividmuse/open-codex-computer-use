# Release 0.1.49

## User Request

> Commit the related changes and push a release.

## Changes

- Bumped Open Computer Use plugin, Swift runtime, smoke suite, Linux runtime, Windows runtime, CLI helper, and tests from `0.1.48` to `0.1.49`.
- Added the `0.1.49` feature release note for macOS open/upload file panel interaction.
- Recorded this release history entry.

## Release Content

`0.1.49` ships the macOS open panel file-click fix: native file lists exposed through `AXContents` / `AXVisibleChildren` now show visible file items, coordinate clicks select native `AXList` children first, and screenshot capture prefers the frontmost overlapping open panel over the underlying app window.

## Validation

- `swift test`
- `node ./scripts/npm/build-packages.mjs --out-dir dist/release/npm-staging-check`
- `./scripts/build-cursor-motion-dmg.sh --configuration release --arch universal --version 0.1.49`
- `node -p "require('./dist/release/npm-staging-check/open-computer-use/package.json').version"` returned `0.1.49`.
- Checked the staged Linux arm64 binary, Windows arm64 binary, missing `optionalDependencies`, and `dist/release/cursor-motion/CursorMotion-0.1.49.dmg`.
