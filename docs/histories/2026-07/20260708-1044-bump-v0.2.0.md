## [2026-07-08 10:44] | Task: Bump release version to v0.2.0

### Execution Context
* **Agent ID**: `Codex`
* **Base Model**: `GPT-5`
* **Runtime**: `macOS local CLI`

### User Query
> 拉取合并后的 main，然后把版本上到 v0.2.0。

### Changes Overview
**Scope:** Release version sources and user-facing release notes.

**Key Actions:**
- Fast-forwarded local `main` to the remote branch after PR #41 was merged.
- Bumped the Open Computer Use plugin manifest, Swift version constant, Linux runtime version, Windows runtime version, smoke initialization version, test initialization fixtures, and helper CLI version from `0.1.54` to `0.2.0`.
- Updated the helper CLI README example path to use the `0.2.0` cache directory.
- Added the `0.2.0` release note covering configurable snapshot tree budgets and the `text_limit` replacement for `show_full_text`.

### Design Intent
The release guide treats the plugin manifest as the npm staging package version source, while the native runtimes and tests carry their own externally visible version strings. Updating them together avoids a release where the tag, npm package, MCP initialize metadata, and CLI `--version` output disagree.

### Files Modified
- `plugins/open-computer-use/.codex-plugin/plugin.json`
- `packages/OpenComputerUseKit/Sources/OpenComputerUseKit/OpenComputerUseVersion.swift`
- `apps/OpenComputerUseLinux/main.go`
- `apps/OpenComputerUseWindows/main.go`
- `apps/OpenComputerUseSmokeSuite/Sources/OpenComputerUseSmokeSuite/main.swift`
- `packages/OpenComputerUseKit/Tests/OpenComputerUseKitTests/OpenComputerUseKitTests.swift`
- `scripts/computer-use-cli/main.go`
- `scripts/computer-use-cli/README.md`
- `docs/releases/feature-release-notes.md`
